import math
import gl
import glscene
import codeview
import input

Scene{
	antialias = true
	Layer{
		w: device.w
		h: device.h
		var  i = 1.
		blarp = 0.1
		pixel: #000
	}
	Codeview{
		x:(device.w / 2 ) -4. * (device.w - mouse.x)
		y:(device.h/ 2 ) - 4. * (device.h - mouse.y)
       
        leftDown = false
       
        beforeMatrix:(){


        }
        
        mouse.leftdown()=>{
          leftDown = true
        }

        mouse.leftup()=>{
          leftDown = false
        }
        
		sx = 1.
		sy = 1.
		font_size = 60
		u_boldness = 0.7 
		ast = style
        mouse = Mouse.new()
	}

}
